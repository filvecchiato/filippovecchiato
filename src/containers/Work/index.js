import React from 'react';

export default class WorkPage extends React.Component{
    render(){
        return (
            <div>
                page with flow of project mockups (Horizon, legal analyser, Q-rator ) 
            </div>
        );
    };
};